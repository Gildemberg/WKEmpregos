import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    container: {
        padding: 30,
        alignItems: 'center',
        backgroundColor: '#ddd',
        flex: 1
    },
    info: {
        marginBottom: 20,
        fontSize: 18,
        textAlign: 'center'
    }
})

export default styles